# JSON Rules Skill

Rules for JSON files:
- Keep files pretty printed with 2-space indentation
- Use stable object keys
- Validate after each edit
- Prefer adding fields via unified diff rather than replacing the whole file
